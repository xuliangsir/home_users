/** * Created by liangxu on 2017/11/21. */'use strict';var knex = require('knex'), db; // 数据库连接const database = require('../config/DB').config;var config = {    client: database.dialect, // 其他可以是pg、sqlite3    connection: {        host: database.host,        user: database.user,        password: database.password,        database: database.database, // 数据库名称        charset: 'utf8',        port: database.port    },    debug: database.debug,    pool: {        min: 0,        max: 10    },    acquireConnectionTimeout: 10000};if (!db) {    db = knex(config);}module.exports = db;