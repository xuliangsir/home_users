/** * Created by liangxu on 2017/11/21. */var config = require("../config/DB").config;var pg = require("pg");var Q = require("q");class DB_Base_Test{    constructor(x){      this.x = x;      this.y = 1;    }    async connect(){        const connectStr = config.pg_connectStr;        const pool = new pg.Pool(config);        return await pool.connect();    }}let base = new DB_Base_Test(100);console.log(base.connect());